# MCP Nexus Voice - Environment Configuration
# Copy this file and rename to .env, then configure your settings

# OpenAI TTS Configuration
# Get your API key from: https://platform.openai.com/account/api-keys
OPENAI_API_KEY=sk-your-openai-api-key-here

# Optional: Default OpenAI voice (if not specified in calls)
# Options: alloy, echo, fable, onyx, nova, shimmer
# Default: echo (clear, direct masculine voice - optimal for Claude)
DEFAULT_TTS_VOICE=echo

# Optional: Default OpenAI model
# Options: tts-1 (faster, standard quality), tts-1-hd (slower, higher quality)
# Default: tts-1
DEFAULT_TTS_MODEL=tts-1

# Optional: Default speech speed
# Range: 0.25 to 4.0 (1.0 = normal speed)
# Default: 1.0
DEFAULT_TTS_SPEED=1.0

# Optional: Temporary file cleanup interval (hours)
# How often to clean up old OpenAI TTS audio files
# Default: 24 hours
# CLEANUP_INTERVAL_HOURS=24

# Development/Debug Settings
# Uncomment to enable additional logging
# DEBUG_VOICE_SELECTION=true
# DEBUG_OPENAI_REQUESTS=true

# Example configurations:
# 
# For premium quality (costs more, slower):
# DEFAULT_TTS_MODEL=tts-1-hd
# DEFAULT_TTS_VOICE=nova
# 
# For fast generation (cheaper, faster):
# DEFAULT_TTS_MODEL=tts-1
# DEFAULT_TTS_VOICE=echo
# 
# For storytelling/presentation:
# DEFAULT_TTS_VOICE=fable
# DEFAULT_TTS_SPEED=0.9
